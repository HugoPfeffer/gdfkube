apiVersion: cluster.open-cluster-management.io/v1beta2
kind: ManagedClusterSet
metadata:
  name: {{ .Values.system.naming.clusterSet }}
  labels:
    {{- toYaml .Values.system.labels | nindent 4 }}
spec:
  clusterSelector:
    selectorType: ExclusiveClusterSetLabel
